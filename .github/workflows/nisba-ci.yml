name: Nisba CI

# Nisba lives in nisba/ and shares nothing with the Moments app at the repository
# root, so this workflow only runs when Nisba itself changes.
on:
  push:
    paths:
      - 'nisba/**'
      - '.github/workflows/nisba-ci.yml'
  pull_request:
    paths:
      - 'nisba/**'
      - '.github/workflows/nisba-ci.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: nisba

jobs:
  verify:
    runs-on: ubuntu-latest

    services:
      postgres:
        # The isolation suite needs a real Postgres. There is no meaningful way
        # to test row-level security against anything else.
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: nisba_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    env:
      ADMIN_DATABASE_URL: postgres://postgres:ci@127.0.0.1:5432/nisba_test
      TEST_ADMIN_DATABASE_URL: postgres://postgres:ci@127.0.0.1:5432/nisba_test
      TEST_DATABASE_URL: postgres://nisba_app:ci-app-password@127.0.0.1:5432/nisba_test
      DATABASE_URL: postgres://nisba_app:ci-app-password@127.0.0.1:5432/nisba_test
      NISBA_APP_DB_PASSWORD: ci-app-password
      AUTH_SECRET: ci-only-secret-not-used-anywhere-real
      AUTH_TRUST_HOST: 'true'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: nisba/package-lock.json

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck

      # The route suite runs against `next start`, so the build has to come
      # first. It also catches anything that typechecks but will not bundle.
      - name: Build
        run: npm run build

      - name: Migrate the test database
        run: npm run db:migrate

      # Isolation is the gate. If org A can read org B by any route, nothing
      # else in this repository matters.
      - name: Test
        run: npm test
